#pragma once

enum class TipoPlanoFinanceiro {
    MENSAL,
    TRIMESTRAL,
    SEMESTRAL,
    ANUAL
};

enum class CondicaoContrato {
    ATIVO,
    CANCELADO
};

enum class Status {
    PAGO,
    PENDENTE
};